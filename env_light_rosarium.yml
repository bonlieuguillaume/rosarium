name: rosarium
channels:
  - conda-forge
dependencies:
  - python=3.11
  # shared by several features
  - ipykernel
  - geopandas
  - shapely
  # download_products
  - rclone
  # polygon_to_swaths_bursts
  - numpy
  - folium
  # aoi_to_slc
  - requests
  - ipyleaflet
  - ipywidgets
  # snap_gpt: band cleanup, GRD split, mosaic (SNAP itself is
  # installed outside conda, see the READMEs of snap_gpt / download_products)
  - gdal
